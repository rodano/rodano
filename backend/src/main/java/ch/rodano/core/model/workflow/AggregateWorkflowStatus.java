package ch.rodano.core.model.workflow;

public class AggregateWorkflowStatus extends WorkflowStatus {
	//create a different cache for aggregate workflow statuses so they can have their own life in the DAO cache alongside other workflow statuses
}
