;configuration should be working without any modification

[DEFAULT]
base=/app/scripts

[scripts]
;control (stop, start, restart) an application
control=control.sh
;backup application
backup=backup.sh
;reset application
reset=reset.sh
;restore application
restore=restore.sh
